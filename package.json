{
  "name": "AppSeum",
  "version": "1.0.0",
  "description": "",
  "main": "back-end/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-prebuild": "cd back-end && npm install",
	  "start": "cd back-end && node index.js",
	  "heroku-postbuild": "cd ./front-end && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "react-file-base64": "^1.0.3"
  }
}